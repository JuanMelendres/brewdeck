spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/brewdeck}
    username: ${DB_USER:brewdeck}
    password: ${DB_PASSWORD:brewdeck}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true